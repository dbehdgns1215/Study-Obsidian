
# String
- `startsWith()`
- `endsWith()`
- `contains()`
- `substring()`
- `charAt()`
- `length()`
- `equals()`


# Array
- `Array.sort()`
- `Array.asList()` -> `Set<String> set = new HashSet<>(Arrays.asList(phone_book));`
	- 배열 안에 있는 모든 전화번호를 해시셋에 넣는 거

